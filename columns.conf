# CSV Column Configuration
# This file is no longer used by the simplified create_repo_list.sh script
# The script now hardcodes the following columns:
# Name, URL, Description, Stars, Open Issues, Open PRs

# This file is kept for reference only
